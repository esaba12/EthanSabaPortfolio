---
phase: 01-design-system-home-page
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - src/app/page.tsx
  - src/app/components/Badge.tsx
  - src/app/components/WaveUnderline.tsx
  - src/app/components/ProjectsGrid.tsx
autonomous: false
requirements:
  - "Home page rebuild (Active #3)"
  - "Motion system (Active #2)"
user_setup: []

must_haves:
  truths:
    - "Home page loads with the charcoal/blueprint-grid canvas, technical display + monospace type, single amber accent — no gradient text, no generic rounded-card-everywhere look remains"
    - "Scrolling the Home page triggers GSAP ScrollTrigger choreography (hero SplitText line reveal + DrawSVG underline stroke on load, Skills/Projects fade+rise on scroll) plus Lenis smooth inertia scroll"
    - "framer-motion still drives hover/tap micro-interactions (Badge focus ring, ProjectsGrid card image hover-scale, WaveUnderline's scroll-linked wiggle)"
    - "User has viewed the running Home page in a browser and approved it as the direction to roll out to Phases 2-5"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Rebuilt Home page: blueprint layout, CoordinateLabel section markers, Panel-wrapped Skills block, Display/Heading typography, GSAP hero + scroll choreography"
      contains: "CoordinateLabel"
    - path: "src/app/components/Badge.tsx"
      provides: "Restyled skill chip: rounded-sm, brand-surface fill, hairline border, Label mono typography"
    - path: "src/app/components/WaveUnderline.tsx"
      provides: "DrawSVG-driven stroke reveal exposed via ref, decoupled from scroll, wiggle mechanism unchanged"
    - path: "src/app/components/ProjectsGrid.tsx"
      provides: "Cards restyled with Panel corner-bracket-on-hover treatment, rounded-sm, filter chips restyled"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/app/components/WaveUnderline.tsx"
      via: "page.tsx's hero GSAP timeline calls the imperative draw() method exposed via WaveUnderline's forwardRef, synced with the SplitText headline reveal"
      pattern: "waveUnderlineRef"
    - from: "src/app/page.tsx"
      to: "src/app/lib/motion-utils.ts"
      via: "page.tsx imports gsap/useGSAP/SplitText/prefersReducedMotion from motion-utils for the hero timeline and ScrollTrigger section reveals"
      pattern: "from './lib/motion-utils'"
    - from: "src/app/components/ProjectsGrid.tsx"
      to: "src/app/components/Panel.tsx"
      via: "each project card renders Panel with bracketsOnHover wrapping the image/content"
      pattern: "<Panel"
---

<objective>
Rebuild the Home page on the Engineering Blueprint system: apply the new layout, typography,
and structural primitives to page.tsx; restyle Badge and ProjectsGrid; wire the one
scroll-stopping hero moment (GSAP SplitText line-draw reveal synced with a DrawSVG-drawn
WaveUnderline stroke) plus restrained ScrollTrigger fade+rise reveals for the Skills and
Projects sections; then get explicit human approval in a live browser before this system
rolls out to Phases 2-5.

Purpose: This is the phase's actual deliverable and proof-of-concept — every prior plan
(01-01's tokens/primitives/motion module, 01-02's shell wiring) exists solely to make this
page buildable. The Roadmap's fifth Phase 1 success criterion requires live human approval,
so this plan does not conclude until that happens.
Output: Rebuilt page.tsx, restyled Badge/WaveUnderline/ProjectsGrid, and a user-approved
running Home page.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system-home-page/01-UI-SPEC.md
</context>

<interfaces>
<!-- Current src/app/page.tsx (full file already read) -->
Current page.tsx is a Server Component (no 'use client', no hooks) with this exact content
that MUST be preserved verbatim (per PROJECT.md's copy-lock — do not change any of these
strings): hero line "Hi. I'm Ethan, and this is the work that I'm proud of.", sub-line "I'm
in my Sophomore year at the University of Michigan learning about Computer Science (Major),
Real Estate (minor), and everything in between.", headings "Skills" / "Programming skills" /
"Real Estate", body text "I'm proficient in Excel and have expericence underwriting
properties and making financial models." (typo "expericence" is in the source — preserve it
exactly, do not "fix" it), heading "Featured Projects", Badge children "C++/C", "Python",
"JavaScript", "Java". It renders `<ProjectsGrid limit={3} />`.

The h1 currently has `text-transparent bg-clip-text bg-gradient-to-r from-cyan-300
via-blue-500 to-violet-600 tracking-tight` — this entire multi-stop gradient-text treatment
must be removed and replaced with solid `text-brand-text` (white). The Skills block currently
sits in a plain `container border-gray-700 border-2 rounded-lg mx-auto px-2 py-2` div — this
gets replaced by the `<Panel>` primitive from Plan 01-01.

From Plan 01-01 (already created — read those files directly, do not re-derive their APIs
from this summary):
- `src/app/lib/motion-utils.ts` exports `gsap`, `ScrollTrigger`, `SplitText`,
  `DrawSVGPlugin`, `useGSAP`, `prefersReducedMotion()`.
- `src/app/components/Panel.tsx` — `{ children, className?, bracketsOnHover? }`.
- `src/app/components/CoordinateLabel.tsx` — `{ children, className? }`.

Current src/app/components/WaveUnderline.tsx (full file already read) drives BOTH its
horizontal wiggle (`xOffset`, via `useTransform` on `scrollYProgress`) AND its stroke draw-in
(`pathLength`, via the same `scrollYProgress`) off the same framer-motion `useScroll` hook
tied to the component's own container bounds. The UI-SPEC requires the stroke draw-in to
happen once on initial page load (synced with the hero headline reveal), while the wiggle
mechanism must stay exactly as it is today (scroll-linked). This plan decouples the two.

Current src/app/components/ProjectsGrid.tsx (full file already read) renders each project as
a `motion.article` with `bg-brand-surface rounded-lg overflow-hidden shadow-card
hover:shadow-lg` and onClick/onKeyDown/role="button"/tabIndex handlers directly on that
element — those interaction handlers and framer-motion variants (`containerVariants`,
`cardVariants`) stay exactly as they are; only the decorative classes and the nested Panel
wrapper change.

Current src/app/components/Badge.tsx props: `{ children: React.ReactNode; className?:
string }` — this exact interface must not change (ProjectsGrid and page.tsx both call
`<Badge>` and must keep working unmodified).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild page.tsx layout/typography and restyle Badge</name>
  <files>src/app/page.tsx, src/app/components/Badge.tsx</files>
  <read_first>src/app/page.tsx, src/app/components/Badge.tsx, src/app/components/Panel.tsx, src/app/components/CoordinateLabel.tsx</read_first>
  <action>
    Add `'use client'` to the top of page.tsx (required starting in Task 2 for GSAP hooks/
    refs — add it now so both tasks in this plan land in the same directive state). Import
    `Panel` from `'./components/Panel'` and `CoordinateLabel` from
    `'./components/CoordinateLabel'`.

    Restructure the three sections (intro, skills, projects), preserving every existing
    text node verbatim per the Interfaces block above:

    Intro section: add a `<CoordinateLabel>{'[ 00 ] INTRO'}</CoordinateLabel>` immediately
    above the h1. On the h1, remove the entire gradient-text utility chain
    (`text-transparent`, `bg-clip-text`, and the gradient-stop classes) and replace with
    `text-brand-text` (solid white) plus the Display typography role: base (mobile, <640px)
    `text-[40px]`, `lg:text-[64px]` (desktop, matching the UI-SPEC's literal "64px desktop
    lg+ / 40px mobile <640px" breakpoint wording — there is no intermediate sm/md value
    specified), `font-display font-bold leading-[1.05] tracking-tight`. Keep the existing
    `<WaveUnderline />` render but capture a ref to it (see Task 2). On the sub-line h3,
    keep as-is (Body role, no change required by the UI-SPEC beyond the sitewide
    line-height already handled in Plan 01-01).

    Skills section: add a `<CoordinateLabel>{'[ 01 ] SKILLS'}</CoordinateLabel>` immediately
    above the "Skills" h2. Apply the Heading typography role to that h2: base (mobile)
    `text-[22px]`, `sm:text-[28px]` (desktop), `font-display font-bold leading-[1.2]` — this
    is a deliberate choice to switch the sm/desktop breakpoint at 640px since the UI-SPEC
    only explicitly names "<640px" as the mobile cutoff for this role. Replace the current
    `container border-gray-700 border-2 rounded-lg mx-auto px-2 py-2` div with `<Panel
    className="mx-auto px-6 py-6">` (not `bracketsOnHover` — this is a static content panel,
    always-visible brackets), keeping the exact same two-column grid of "Programming
    skills"/Badge-list and "Real Estate"/paragraph inside it unchanged. The inner "Programming
    skills" and "Real Estate" h3 elements keep their current sizing (they are sub-labels
    inside the panel, not a Heading-role section title — UI-SPEC does not call these out for
    resizing).

    Projects section: add a `<CoordinateLabel>{'[ 02 ] PROJECTS'}</CoordinateLabel>`
    immediately above the "Featured Projects" h2, and apply the same Heading typography role
    (base `text-[22px]`, `sm:text-[28px]`, `font-display font-bold leading-[1.2]`) used for
    "Skills". Leave `<ProjectsGrid limit={3} />` as the child (Task 3 restyles that
    component's internals separately).

    In Badge.tsx: change `rounded-full` to `rounded-sm` (2px, within the UI-SPEC's 4px cap),
    change `bg-brand-surface-light` to `bg-brand-surface`, change `ring-1 ring-brand-border`
    to `border border-brand-border` (a literal hairline border, matching "hairline neutral"
    wording), and replace `text-sm font-medium` with the Label typography role: `font-mono
    text-[13px] leading-[1.4] uppercase tracking-[0.04em]`. Keep the `{children}` prop,
    `className` prop, `tabIndex={0}`, and the existing focus-ring classes
    (`focus:ring-2 focus:ring-brand-accent focus:ring-offset-2
    focus:ring-offset-brand-bg`) exactly as they are — the component's public interface
    does not change.
  </action>
  <verify>
    <automated>cd /Users/ethansaba/code/EthanSabaPortfolio && npx tsc --noEmit && grep -q "'use client'" src/app/page.tsx && grep -q "CoordinateLabel" src/app/page.tsx && grep -q "<Panel" src/app/page.tsx && ! grep -q "bg-clip-text" src/app/page.tsx && grep -q "Hi. I" src/app/page.tsx && grep -q "expericence" src/app/page.tsx && grep -q "rounded-sm" src/app/components/Badge.tsx && ! grep -q "rounded-full" src/app/components/Badge.tsx</automated>
  </verify>
  <done>page.tsx is a Client Component using Panel/CoordinateLabel, the gradient-text h1 treatment is gone (replaced with solid Display-role white text), all existing copy is preserved verbatim, and Badge.tsx uses rounded-sm/brand-surface/hairline border/mono Label typography while keeping its existing props interface</done>
  <acceptance_criteria>
    - page.tsx has 'use client' at the top
    - page.tsx renders 3 CoordinateLabel section markers ([ 00 ] INTRO, [ 01 ] SKILLS, [ 02 ] PROJECTS)
    - page.tsx wraps the Skills content in Panel (not the old rounded-lg border div)
    - h1 has no bg-clip-text/gradient classes and uses solid text-brand-text + Display role sizing
    - every existing text string (hero line, sub-line, headings, the "expericence" typo) is byte-identical to before
    - Badge.tsx uses rounded-sm, bg-brand-surface, border border-brand-border, and mono Label typography
    - Badge.tsx's exported props interface ({children, className?}) is unchanged
    - `npx tsc --noEmit` reports zero errors
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: GSAP hero choreography (SplitText + DrawSVG) and ScrollTrigger section reveals</name>
  <files>src/app/page.tsx, src/app/components/WaveUnderline.tsx</files>
  <read_first>src/app/components/WaveUnderline.tsx, src/app/lib/motion-utils.ts</read_first>
  <behavior>
    - On initial page load, with no reduced-motion preference set: the hero h1 reveals via
      SplitText's `mask: "lines"` wipe (lines start translated down/clipped and animate to
      their resting position), and the WaveUnderline stroke draws in via DrawSVG
      (0% -> 100%) starting partway through the headline reveal, both finishing within a
      1.1-1.4s window using `power3.out` easing. This runs exactly once and is never
      repeated elsewhere on the page.
    - On initial page load, with `prefers-reduced-motion: reduce` set: the headline and
      underline render immediately in their final state — no SplitText instance is created,
      no DrawSVG tween runs.
    - A `SCROLL` cue (CoordinateLabel-styled text + a short vertical line beneath it) fades
      in after the hero reveal completes and fades out the first time the user scrolls (once
      only). Under reduced motion, the cue's line does not animate/pulse and its fade-out is
      instant (duration 0) rather than tweened.
    - When the Skills Panel and the Featured Projects section each scroll to `top 80%` of the
      viewport, they animate from `opacity: 0, y: 8px` to `opacity: 1, y: 0` over ~0.4s with
      `power2.out` easing — same treatment for both, no per-section bespoke variation. Under
      reduced motion, both render at their final opacity/position with no ScrollTrigger
      animation attached.
    - The blueprint grid, footer, and navbar are never touched by any GSAP effect added in
      this task — only the hero elements and the two section reveals.
  </behavior>
  <action>
    In WaveUnderline.tsx: keep the existing `containerRef`, `prefersReducedMotion` state, and
    the `xOffset` wiggle mechanism (driven by `useTransform(scrollYProgress, ...)` on the
    wrapping `<motion.svg>`) completely unchanged — this is the "scroll-linked wiggle" the
    UI-SPEC says to keep as-is. Remove the `pathLength` motion value / `useTransform` call
    that currently drives the stroke draw-in from scroll. Convert the component to a
    `forwardRef` that exposes an imperative handle via `useImperativeHandle`: a single
    `draw(options?: { duration?: number; ease?: string })` method that runs a DrawSVG tween
    (imported from `'../lib/motion-utils'`) on the path element from `drawSVG: '0%'` to
    `drawSVG: '100%'`, using `options?.duration ?? 1.2` and `options?.ease ?? 'power3.out'`.
    Add a `pathRef` (plain `useRef<SVGPathElement>`) attached to the existing
    `<motion.path>` element to serve as the DrawSVG tween target. On mount, via a `useEffect`
    (or `useGSAP`), call `gsap.set(pathRef.current, { drawSVG: prefersReducedMotion ? '100%'
    : '0%' })` so the path starts in the correct initial state — fully drawn immediately if
    reduced motion is on (in which case `draw()` becomes a no-op that resolves immediately),
    or hidden and awaiting the imperative `draw()` call otherwise. Export the component with
    `React.forwardRef`.

    In page.tsx: import `gsap`, `useGSAP`, `SplitText`, `prefersReducedMotion` from
    `'./lib/motion-utils'`. Add a `heroRef` (wraps the intro section), a `waveUnderlineRef`
    (passed as the `ref` prop to `<WaveUnderline ref={waveUnderlineRef} />`), a
    `skillsRef` (on the Panel wrapping Skills), a `projectsRef` (on the Featured Projects
    section container), and a `scrollCueRef`.

    Inside a single `useGSAP(() => { ... }, { scope: heroRef })` call: if
    `prefersReducedMotion()` returns true, skip straight to rendering the SCROLL cue in its
    resting visible state and attach a one-time scroll listener that instantly hides it
    (`opacity: 0`, no tween) — then return early, running none of the animated branches
    below. Otherwise: create a `SplitText` instance on the h1 with `{ type: 'lines', mask:
    'lines' }` (this auto-wraps each line in an overflow-clip container, producing the
    "masked... clip-path reveal, not a fade" effect the UI-SPEC requires); build a
    `gsap.timeline({ defaults: { ease: 'power3.out' } })` that tweens the split lines from
    `yPercent: 100` to their resting position over `duration: 1.2` with a small
    `stagger: 0.08` between lines, then `.call()`s `waveUnderlineRef.current?.draw({
    duration: 1.2, ease: 'power3.out' })` positioned to start partway through the line
    reveal (e.g. timeline position `"<0.3"`) so both finish within the 1.1-1.4s window: then
    animates the SCROLL cue in (`opacity: 0 -> 1`) after the timeline completes. Attach a
    `{ once: true }` scroll listener (via `window.addEventListener('scroll', handler, {
    once: true })`, or an equivalent one-shot ScrollTrigger) that fades the SCROLL cue out
    (`gsap.to(scrollCueRef.current, { opacity: 0, duration: 0.3 })`) the first time the user
    scrolls, and remove that listener after it fires.

    In a second `useGSAP` call scoped to the page (or the same call, after the hero
    timeline): if not reduced motion, register two `ScrollTrigger`-driven `gsap.from()` tweens
    — one targeting `skillsRef.current`, one targeting `projectsRef.current` — each
    `{ opacity: 0, y: 8, duration: 0.4, ease: 'power2.out', scrollTrigger: { trigger: <that
    element>, start: 'top 80%' } }`. Do not add any ScrollTrigger/animation to the
    BlueprintGrid, Navbar, or Footer.

    Render the SCROLL cue as a small block near the bottom of the hero section: a
    `CoordinateLabel` reading `SCROLL` plus a short vertical line element (a 1px-wide,
    ~24px-tall div using `bg-brand-accent` — this is the one explicitly-permitted use of the
    accent color as "the single-instance line-draw stroke used in the one big early
    scroll-triggered wow reveal," extended here to its accompanying scroll cue since both are
    part of the same initial-load moment), wrapped in a ref (`scrollCueRef`) so the reveal/
    fade-out logic above can target it.
  </action>
  <verify>
    <automated>cd /Users/ethansaba/code/EthanSabaPortfolio && npx tsc --noEmit && grep -q "forwardRef" src/app/components/WaveUnderline.tsx && grep -q "useImperativeHandle" src/app/components/WaveUnderline.tsx && grep -q "drawSVG" src/app/components/WaveUnderline.tsx && grep -q "SplitText" src/app/page.tsx && grep -q "mask: 'lines'" src/app/page.tsx && grep -q "ScrollTrigger" src/app/page.tsx && grep -q "top 80%" src/app/page.tsx && grep -q "prefersReducedMotion" src/app/page.tsx && grep -q "SCROLL" src/app/page.tsx</automated>
  </verify>
  <done>WaveUnderline exposes an imperative draw() method via forwardRef/useImperativeHandle driven by DrawSVG, its wiggle mechanism is untouched, and page.tsx runs a reduced-motion-gated SplitText hero timeline synced with WaveUnderline's draw(), a SCROLL cue that fades on first scroll, and ScrollTrigger fade+rise reveals on the Skills Panel and Featured Projects section</done>
  <acceptance_criteria>
    - WaveUnderline.tsx is wrapped in React.forwardRef and exposes draw() via useImperativeHandle
    - WaveUnderline.tsx's xOffset wiggle transform is unchanged from before this task
    - WaveUnderline.tsx no longer drives stroke draw-in from scrollYProgress
    - page.tsx creates a SplitText instance with type 'lines' and mask 'lines' on the hero h1
    - page.tsx's hero timeline calls waveUnderlineRef.current?.draw(...)
    - page.tsx has two ScrollTrigger reveals using start: 'top 80%', one for Skills, one for Projects
    - every animated branch is gated behind prefersReducedMotion() with a full non-animated fallback
    - no ScrollTrigger/tween targets BlueprintGrid, Navbar, or Footer
    - `npx tsc --noEmit` reports zero errors
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Restyle ProjectsGrid cards onto the Panel treatment</name>
  <files>src/app/components/ProjectsGrid.tsx</files>
  <read_first>src/app/components/ProjectsGrid.tsx, src/app/components/Panel.tsx</read_first>
  <action>
    Import `Panel` from `'./Panel'`. On each `motion.article` card: remove the decorative
    classes `bg-brand-surface rounded-lg overflow-hidden shadow-card hover:shadow-lg` (these
    move onto the nested Panel instead) but keep every interaction-related class and
    attribute exactly as-is — `group`, `focus-within:ring-2 focus-within:ring-brand-accent
    focus-within:ring-offset-2 focus-within:ring-offset-brand-bg`, `cursor-pointer`,
    `tabIndex={0}`, `onClick`, `onKeyDown`, `role="button"`, `aria-label`, and all of the
    `motion.article`'s `variants`/`initial`/`animate`/`exit` framer-motion props (these
    already provide the card entrance stagger — do not modify them). Inside the
    `motion.article`, wrap the existing image div + content div (everything currently
    inside the article) in `<Panel bracketsOnHover className="h-full overflow-hidden flex
    flex-col">` — `bracketsOnHover` matches the UI-SPEC's "corner brackets on hover, not
    resting state, to avoid visual noise across 3 cards at once." Panel supplies
    `bg-brand-surface`, the hairline border, `rounded-sm`, and the shadow can be reapplied on
    the Panel wrapper via its `className` prop: append `shadow-card hover:shadow-card-hover
    transition-shadow duration-300` (replacing the old ad-hoc `hover:shadow-lg` with the
    project's own defined `card-hover` shadow token).

    On the two filter buttons ("All" and each tag button): change `rounded-full` to
    `rounded-sm`, change the inactive-state `bg-white/5` to `bg-brand-surface` (using the
    design token instead of an arbitrary opacity value), and switch the text treatment to
    the Label mono role (`font-mono text-[13px] uppercase tracking-[0.04em]`) alongside the
    existing `px-4 py-2` sizing — the active-state `bg-brand-accent text-white` classes stay
    unchanged (already amber via the Plan 01-01 token). Do not change the `useState`/
    `useMemo` filtering logic, the `allTags`/`filteredProjects` computation, the empty-state
    block, or any data shape — only the className strings identified above.
  </action>
  <verify>
    <automated>cd /Users/ethansaba/code/EthanSabaPortfolio && npx tsc --noEmit && grep -q "import Panel from './Panel'" src/app/components/ProjectsGrid.tsx && grep -q "<Panel bracketsOnHover" src/app/components/ProjectsGrid.tsx && grep -q "shadow-card-hover" src/app/components/ProjectsGrid.tsx && grep -q "rounded-sm" src/app/components/ProjectsGrid.tsx && grep -q "role=\"button\"" src/app/components/ProjectsGrid.tsx && grep -q "onKeyDown" src/app/components/ProjectsGrid.tsx</automated>
  </verify>
  <done>ProjectsGrid cards render Panel with bracketsOnHover for their visual chrome while keeping every existing interaction handler/attribute and framer-motion variant unchanged, filter buttons use rounded-sm + mono Label styling, and no filtering logic or data handling changed</done>
  <acceptance_criteria>
    - ProjectsGrid.tsx imports and renders Panel with bracketsOnHover inside each motion.article
    - motion.article still has onClick, onKeyDown, role="button", tabIndex={0}, aria-label, and all framer-motion variants unchanged
    - Panel wrapper uses shadow-card/hover:shadow-card-hover (not the old hover:shadow-lg)
    - filter buttons use rounded-sm instead of rounded-full and mono Label text styling
    - filtering logic (useState/useMemo/allTags/filteredProjects) is byte-identical to before
    - `npx tsc --noEmit` reports zero errors
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Live browser approval of the rebuilt Home page</name>
  <files>src/app/page.tsx, src/app/components/Badge.tsx, src/app/components/WaveUnderline.tsx, src/app/components/ProjectsGrid.tsx, src/app/layout.tsx, src/app/components/navbar.tsx, src/app/components/footer.tsx</files>
  <read_first>.planning/ROADMAP.md</read_first>
  <action>Run the dev server and walk the human through the 8-step verification checklist below against all 5 Phase 1 Roadmap success criteria; wait for explicit approval before the phase is considered complete. This is a human-judgment gate, not automatable — visual/motion quality and "does this feel right" requires a live browser.</action>
  <what-built>
    The complete Engineering Blueprint design system (Plan 01-01), the wired shared shell
    (Plan 01-02), and the fully rebuilt Home page with its GSAP hero choreography (this
    plan's Tasks 1-3) are all in place. This is the single checkpoint for the whole phase —
    it verifies the assembled result against all 5 Phase 1 Roadmap success criteria at once.
  </what-built>
  <how-to-verify>
    1. Run `npm run dev` and open http://localhost:3000 in a browser.
    2. Confirm the visual system: charcoal background with a faint persistent grid texture,
       bold technical display type on the hero headline, monospace section-index labels
       (`[ 00 ] INTRO`, `[ 01 ] SKILLS`, `[ 02 ] PROJECTS`), a single amber accent color used
       sparingly — no gradient text anywhere, no rounded-lg/rounded-full "SaaS card" look on
       the Skills panel or project cards.
    3. Reload the page and watch the hero headline reveal (a line-wipe, not a fade) with the
       underline stroke drawing in beneath it, finishing in roughly a second — this should
       happen once, automatically, without scrolling.
    4. Scroll down: confirm smooth, slightly weighted (Lenis) inertia scrolling (not the
       browser's native instant scroll), the SCROLL cue fades out on your first scroll input,
       and the Skills panel and Featured Projects section each fade+rise into view as they
       cross into the viewport.
    5. Hover a project card: confirm corner brackets appear on hover (not visible at rest)
       and the project image scales slightly — these are the existing framer-motion
       micro-interactions, unchanged in mechanism.
    6. Tab through the page with the keyboard: confirm Badge chips and the nav still show a
       visible amber focus ring.
    7. Confirm the navbar/footer look like part of the same system (hairline borders, no
       glow/blur pill), not a separate visual language.
    8. Optional: in your OS/browser accessibility settings, enable "reduce motion," reload,
       and confirm the hero and section content appear immediately with no animation.
  </how-to-verify>
  <resume-signal>Type "approved" to accept this as the Phase 1 reference implementation for Phases 2-5, or describe what needs to change</resume-signal>
  <verify>
    <automated>N/A — human visual/interactive judgment only; `npm run build` in this plan's &lt;verification&gt; block already confirms the production build compiles</automated>
  </verify>
  <done>Human has typed "approved" (or equivalent explicit confirmation) accepting the Home page as the Phase 1 reference implementation</done>
  <acceptance_criteria>User has explicitly approved the running Home page against all 5 Phase 1 Roadmap success criteria before this phase is considered complete</acceptance_criteria>
</task>

</tasks>

## Artifacts This Phase Produces

(Full Phase 1 inventory — see 01-01-PLAN.md's "Artifacts This Phase Produces" table for the
complete list across all three plans.) This plan modifies: src/app/page.tsx,
src/app/components/Badge.tsx, src/app/components/WaveUnderline.tsx,
src/app/components/ProjectsGrid.tsx.

<threat_model>
## Trust Boundaries

No new trust boundaries — this plan only restyles/re-choreographs existing client-rendered
content. No new external packages (all GSAP plugins used here — SplitText, DrawSVGPlugin —
come from the `gsap` package already installed and legitimacy-checked in Plan 01-01, not a
separate install). No user input, no data persistence, no auth.

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04 | Denial of Service (perf/accessibility) | Hero SplitText/DrawSVG timeline + ScrollTrigger section reveals in page.tsx | low | mitigate | Every animated branch is gated behind `prefersReducedMotion()` from motion-utils.ts; when reduced motion is preferred, all elements render in their final state instantly with zero GSAP tweens created |
| T-01-05 | Information Disclosure | Home page content | low | accept | No new data is rendered — same `projects.json`-sourced content as before, only restyled; no secrets or user data involved |
| T-01-SC | Tampering | npm/pip/cargo installs | high | mitigate | No new installs in this plan — SplitText/DrawSVGPlugin ship inside the already-installed `gsap` package; supply-chain risk was mitigated via the blocking checkpoint in Plan 01-01 (T-01-01); carried forward for traceability only |

</threat_model>

<verification>
Run `npx tsc --noEmit` after Tasks 1-3 complete — must report zero errors. Run `npm run
build` once before the Task 4 checkpoint to confirm a real production build succeeds (not
just type-checking). Then run `npm run dev` for the Task 4 live-browser checkpoint itself.
</verification>

<success_criteria>
- Home page loads with the blueprint canvas/type/accent system, zero gradient text, zero rounded-lg/rounded-full "SaaS card" styling remaining
- Hero reveals via GSAP SplitText line-wipe synced with a DrawSVG-drawn WaveUnderline stroke, once, on load
- Skills Panel and Featured Projects section reveal via ScrollTrigger fade+rise at top 80%
- Lenis smooth scroll is active site-wide (verified via Plan 01-02's mount + this plan's live check)
- framer-motion still drives Badge focus, ProjectsGrid card hover-scale, and WaveUnderline's wiggle
- All animated effects fall back to an instant final state under prefers-reduced-motion
- `npx tsc --noEmit` and `npm run build` both succeed
- User has typed "approved" at the Task 4 checkpoint
</success_criteria>

<output>
Create `.planning/phases/01-design-system-home-page/01-03-SUMMARY.md` when done
</output>
